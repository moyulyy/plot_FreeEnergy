# Energy Profile Studio · 运行依赖
# 安装:  pip install -r requirements.txt
#
# 必需 ------------------------------------------------------------------
numpy>=1.24
matplotlib>=3.6
PySide6>=6.5

# 可选 ------------------------------------------------------------------
# 能量数值文字自动避让。不安装时程序使用内置的轻量避让算法（仅纵向错开）。
# adjustText>=0.8
#
# Excel 读取的兜底解析。程序内置标准库 xlsx 解析器，通常无需安装。
# openpyxl>=3.0
